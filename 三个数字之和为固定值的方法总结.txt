1:这个算法应该从一个从简单到复杂的思索过程。
2:比如一看到这个问题，就应该想到sum[i][j][k]代表三个数字的和，复杂度为(O(n^3))
3:应该涉及到大小问题，排序一下应该会更好分析，所以进行排序。之后更优的算法就出来了。
	排序之后，从一端依次取一个值，另外一端就变成了排好序的两个数为固定值的问题。
	排好序的两个数的和为固定值的算法非常简单，
		如果小于sum,将小的指针往大的方向移动。
		如果大于sum,将大的指针往小的方向移动。
		如果等于sum,输出。